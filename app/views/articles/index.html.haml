.articles
  %h1 記事一覧

  = link_to '記事を作成する', new_article_path

  - @articles.each do |article|
    %article{ class: dom_id(article) }
      %header
        %h2= article.title
      %section
        = simple_format article.body
      %footer
        %time= l article.created_at
